<Window x:Class="DeskLite.TodoListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="全部待办"
        Width="420"
        Height="520"
        MinWidth="360"
        MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="#181C24"
        Foreground="#F5F7FA"
        FontSize="13">
    <Border x:Name="RootBorder" Padding="16" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="HeaderText" Grid.Row="0" Text="待办清单" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />

            <StackPanel x:Name="FilterPanel" Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="FilterActiveBtn" Content="进行中" Padding="12,5" Margin="0,0,6,0"
                        Click="FilterActive_Click" Cursor="Hand" BorderThickness="0" />
                <Button x:Name="FilterDoneBtn" Content="已完成" Padding="12,5" Margin="0,0,6,0"
                        Click="FilterDone_Click" Cursor="Hand" BorderThickness="0" />
                <Button x:Name="FilterAllBtn" Content="全部" Padding="12,5"
                        Click="FilterAll_Click" Cursor="Hand" BorderThickness="0" />
            </StackPanel>

            <TextBox x:Name="SearchBox" Grid.Row="2" Height="32" Padding="10,6" Margin="0,0,0,10"
                     TextChanged="SearchBox_TextChanged" />

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="TodoItemsPanel" />
            </ScrollViewer>

            <TextBlock x:Name="EmptyHintText" Grid.Row="3" Text="暂无待办" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />

            <TextBlock x:Name="FooterHintText" Grid.Row="4" Text="双击标题可编辑 · 勾选可完成或恢复"
                       FontSize="11" Margin="0,10,0,0" TextWrapping="Wrap" />
        </Grid>
    </Border>
</Window>
